# Web
Flask>=3.0
Jinja2>=3.1

# Data + ML (consumed by webapp at inference time AND by training)
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# NLP utilities used in shared/
nltk>=3.8

# Notebook plotting
matplotlib>=3.7

# Product image generation (offline content prep)
Pillow>=10.0

# Notebook (training only, but in the same env)
jupyter>=1.0

# Dev
pytest>=8.0
